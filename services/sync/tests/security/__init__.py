"""Security tests (spec section 11.3).

These are the tests the specification calls prioritary: they verify that Row
Level Security, credential encryption and secret hygiene actually hold, rather
than being assumed to hold.
"""
