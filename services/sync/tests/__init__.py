"""Test suite for the Sunder sync service.

A package (rather than a bare directory) so that helper modules and the shared
constants in `conftest` can be imported explicitly, e.g.
`from tests.conftest import TEST_KEY`.
"""
